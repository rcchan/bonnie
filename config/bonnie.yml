defaults: &defaults
  default_user_approved: true
  version: 0.0.3
  
development:
  <<: *defaults
  
test:
  <<: *defaults
  default_user_approved: false
  
production:
  <<: *defaults
  default_user_approved: false
